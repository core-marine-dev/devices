{
  "name": "@coremarine/TODO:-nodered",
  "private": true,
  "version": "0.0.1",
  "author": "CoreMarine",
  "license": "MIT",
  "description": "TODO:",
  "homepage": "https://github.com/core-marine-dev/devices/tree/main/packages/TODO:-nodered",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/core-marine-dev/devices.git",
    "directory": "packages/TODO:-nodered"
  },
  "bugs": {
    "url": "https://github.com/core-marine-dev/devices/issues"
  },
  "keywords": [
    "TODO:"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "node-red": {
    "version": ">=4.0.0",
    "nodes": {
      "cma-TODO:": "dist/parser.js"
    }
  },
  "main": "dist/parser.js",
  "files": [
    "dist",
    "examples"
  ],
  "scripts": {
    "build": "pnpm run format && tsup && node copy-assets.mjs",
    "lint": "eslint",
    "format": "eslint --fix",
    "test": "node --import tsx --test \"tests/**/*.test.ts\"",
    "test:unit": "node --import tsx --test \"tests/*.unit.test.ts\"",
    "test:integration": "node --import tsx --test \"tests/*.integration.test.ts\"",
    "dev": "pnpm run build && node dev-server.mjs"
  },
  "dependencies": {
    "@coremarine/TODO:": "workspace:^"
  },
  "devDependencies": {
    "@types/node-red": "1.3.5",
    "tsx": "4.23.1"
  }
}
